# set up git 

```npm install --save```

## to start application

``` node index.js ```

## nodemon

``` npm install -g nodemon```
``` nodemon ```

## curl, a command line tool 

that allows me to make requests

``` curl localhost:8081```

